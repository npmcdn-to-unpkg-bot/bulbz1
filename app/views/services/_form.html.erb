
<%= form_for(@service) do |f| %>
  <% if @service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service.errors.count, "error") %> prohibited this app from being saved:</h2>

      <ul>
      <% @service.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <h4><%= f.text_field(:title, :class => "comment_input",:placeholder => "NAME YOUR BULB (eg. UBER)") %></h4>
  <h4>is an <span class="bulb_type">SERVICE</span> that helps</h4>
  <h4><%= f.text_field(:target, :class => "comment_input", :placeholder => "DEFINE AN AUDIENCE (eg. BUSINESS PEOPLE)") %> </h4>
  <h4><%= f.text_area(:description, :class => "text_area_small2", :placeholder => "Decribe it in your own words.") %></h4>
  <%= f.submit("CREATE BULB", :class => "submit_button comment_button") %>
<% end %>
