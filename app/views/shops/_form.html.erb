
<%= form_for(@shop) do |f| %>
  <% if @shop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shop.errors.count, "error") %> prohibited this app from being saved:</h2>

      <ul>
      <% @shop.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <h3><%= f.text_field :title %></h3>
  <h3>is a <span class="bulb_type">SHOP</span> that sells</h3>
  <h3><%= f.text_area(:description, :class => "text_area_small") %></h3>
  <h3>to</h3>
  <h3><%= f.text_field :target %></h3>
  <%= f.submit("CREATE BULB", :class => "submit_button") %>
<% end %>
