<%= form_for(@platform) do |f| %>
  <% if @platform.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@platform.errors.count, "error") %> prohibited this platform from being saved:</h2>

      <ul>
      <% @platform.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <h4><%= f.text_field(:title, :class => "comment_input",:placeholder => "NAME YOUR BULB (eg. AIRBNB)") %></h4>
  <h4>is a <span class="bulb_type">PLATFORM</span> that connects</h4>
  <h4><%= f.text_field(:target1, :class => "comment_input",:placeholder => "DEFINE AUDIENCE 1" ) %></h4>
  <h4 style="padding-left: 250px"><span class="bulb_type">+</span></h4>
  <h4><%= f.text_field(:target2, :class => "comment_input",:placeholder => "DEFINE AUDIENCE 2" ) %></h4>
  <h4><%= f.text_area(:description, :class => "text_area_small2", :placeholder => "Decribe it in your own words.") %></h4>
  <%= f.submit("CREATE BULB", :class => "submit_button comment_button", data: {disable_with: "Starting Machine Learning..."}) %>


<% end %>
